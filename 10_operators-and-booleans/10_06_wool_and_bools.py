# Write a code snippet that gives a name to a `sheep`
# and uses a boolean value to define whether it has `wool` or not.

sheep_name = "joe"
statement = "all sheeps have wool"

answer = "wool" in statement

print(f"It is {answer} that {sheep_name} has wool.")